---
name: "GitHub Super Linter"

on:
  schedule:
    - cron: "0 12 * * *"
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  call-workflow-linter:
    uses: pandemonium1986/.github/.github/workflows/linter.yml@main
    secrets:
      github-token: inherit
